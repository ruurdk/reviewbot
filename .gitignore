__pycache__/
*.pyc
# Real credentials. .env.example is the committed template.
.env
.env.*
!.env.example
# Ingested GitHub responses and assembled PRs: reproducible from the pinned
# SHAs, and large. The sequence manifest itself IS committed.
data/cache/
data/prs/
# Run ledgers: the frozen run is committed deliberately, drafts are not.
runs/*/
!runs/frozen/
runs/*.log

# web app build output and deps
web/node_modules/
web/dist/
web/dist-smoke/

# Local redis-py clone for --checkout source reads (reproducible: git clone).
.checkouts/
